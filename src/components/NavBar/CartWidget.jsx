

const CartWidget = () => {

    return(
        <div style={{display: "flex", width: 28, justifyContent: "space-between", alignItems: "center"}}>
            🛒
            <span style={{fontWeight: "bold", fontSize: "15px", color: "darkblue"}}>3</span>
        </div>
    )

}

export default CartWidget;